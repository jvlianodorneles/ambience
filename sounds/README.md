# Custom Sounds Directory

Drop your own `.ogg`, `.mp3`, `.wav`, or `.flac` audio loops into this folder (`sounds/`).

The Ambience Studio plugin will automatically detect them and add them to your Soundscapes menu!
